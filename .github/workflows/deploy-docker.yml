
# Autogenerated by @clabroche/common-workflows package
# If you must edit this file, edit files in .github/workflow-*.yml
# Then launch "yarn ci:generate"

name: Publish docker
on:
  push:
    branches: 
      - 'deploy-docker'

jobs:
  checks:
    runs-on: ubuntu-latest
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}

    steps:


      - name: Checkout
        uses: actions/checkout@v3

      - name: Config git user
        run: |
          git config --global user.name "${{ github.actor }}"
          git config --global user.email "${{ github.actor }}@users.noreply.github.com"
  
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ vars.DOCKER_HUB_USER }}
          password: ${{ secrets.NPM_TOKEN }}

      - name: Build and push base node 18 image
        uses: docker/build-push-action@v5
        with:
          tags: ghcr.io/clabroche/monorepo/infra-docker-base-node-18:latest
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          context: ./docker
          file: ./docker/Dockerfile.base-node-18
          push: true
          build-args: |
            NPM_TOKEN=${{secrets.NPM_TOKEN}}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push build image
        uses: docker/build-push-action@v5
        with:
          tags: ghcr.io/clabroche/monorepo/infra-docker-build-node:latest
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          context: ./docker
          file: ./docker/Dockerfile.build
          push: true
          build-args: |
            NPM_TOKEN=${{secrets.NPM_TOKEN}}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push chromium image
        uses: docker/build-push-action@v5
        with:
          tags: ghcr.io/clabroche/monorepo/infra-docker-chromium-node:latest
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          context: ./docker
          file: ./docker/Dockerfile.chromium
          push: true
          build-args: |
            NPM_TOKEN=${{secrets.NPM_TOKEN}}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Rollout image
        uses: docker/build-push-action@v5
        with:
          tags: ghcr.io/clabroche/monorepo/infra-docker-rollout-node-18:latest
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          context: ./docker
          file: ./docker/Dockerfile.rollout-node-18
          push: true
          build-args: |
            NPM_TOKEN=${{secrets.NPM_TOKEN}}
          cache-from: type=gha
          cache-to: type=gha,mode=max